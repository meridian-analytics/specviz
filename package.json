{
  "name": "@meridian-analytics/specviz-monorepo",
  "type": "module",
  "scripts": {
    "check": "bun --filter '*' check",
    "test": "bun --filter '*' test",
    "tsc": "bun --filter '*' tsc"
  },
  "workspaces": ["examples/*", "packages/*"]
}
